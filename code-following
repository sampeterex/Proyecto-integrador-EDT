System.out.println("Ingrese los libros para llenar el estante (" + (n * n) + " espacios disponibles).");

        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                System.out.print("Nombre del libro para la posición [" + i + "][" + j + "]: ");
                String nombreLibro = sc.nextLine();

                System.out.print("Número del libro: ");
                String numeroLibro = sc.nextLine();

               
                estante[i][j] = numeroLibro + " - " + nombreLibro;
            }
        }
